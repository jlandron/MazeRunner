using System.Collections.Generic;
using UnityEngine;

public class LightMover : MonoBehaviour {
    [SerializeField] float lightMoveDelay = 10;

    void Update( ) {
        //transform.Rotate( 0, lightMoveDelay * Time.deltaTime, 0 );
    }
}
